# tooling

Repo-level engineering support lives here.

Examples include shared configs, scripts, and generators.

Development-only benchmark seeds for agent-loop replay experiments live under
`tooling/benchmarks/`.
