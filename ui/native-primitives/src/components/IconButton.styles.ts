import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  iconButton: {
      alignItems: 'center',
      justifyContent: 'center',
      borderWidth: 1,
    },
});

export { styles };
