import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  progressBarFill: {
      height: '100%',
      borderRadius: 3,
    },
  progressBarTrack: {
      height: 6,
      borderRadius: 3,
      overflow: 'hidden',
    },
});

export { styles };
